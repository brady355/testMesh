# Optional Offline Source Cache

This directory is intentionally empty in Git.

For a normal clean install, the setup scripts download or clone the required upstream sources when they are missing. If you need a fully offline field install, seed this directory after cloning with the optional archives named in `TROUBLESHOOTING.md`.
